<Window x:Class="SWE_TourManager.Views.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SWE_TourManager.Views"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0" Grid.RowSpan="3">
            <Button>Create Tour</Button>
            <Button>Modify Tour</Button>
            <Button>Copy Tour</Button>
            <Button>Import Tour</Button>
            <Button>Export Tour</Button>
            <Button>Print Tour</Button>
            <Button>Delete Tour</Button>
        </StackPanel>

        <ListView Grid.Column="1" Grid.Row="1">
            <ListViewItem>tour 1</ListViewItem>
        </ListView>
        
    </Grid>
</Window>
